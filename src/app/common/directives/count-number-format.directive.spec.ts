import { CountNumberFormatDirective } from './count-number-format.directive';

describe('CountNumberFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CountNumberFormatDirective();
    expect(directive).toBeTruthy();
  });
});
